#!/bin/bash

# Post-installation

set -e
if [ "$1" = "configure" ] ; then
    ln -s /usr/bin/tetris /usr/local/bin/tetris  # creates a symbolic link, so that the "tetrust" command can be used from the terminal.
fi


